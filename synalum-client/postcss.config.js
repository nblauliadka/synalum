export default {
  plugins: {
    '@tailwindcss/postcss': {}, // Plugin Tailwind v4 untuk memproses class utility lewat PostCSS
    autoprefixer: {}, // Otomatis tambahkan prefix CSS agar kompatibel lintas browser
  },
}
